123456789
12


docker container run \
  --name jenkins \
  --detach \
  --network jenkins \
  --env DOCKER_HOST=tcp://docker:2376 \
  --env DOCKER_CERT_PATH=/certs/client \
  --env DOCKER_TLS_VERIFY=1 \
  --publish 8081:8080 \
  --publish 50000:50000 \
  --volume jenkins-data:/var/jenkins_home \
  --volume jenkins-docker-certs:/certs/client:ro \
  -u 0 jenkinsci/blueocean
  
  
  
  
docker run \
  --name jenkins \
  --detach \
  -p 8001:8080 \
  -v jenkins-data:/var/docker_jenkins_home \
  jenkinsci/blueocean